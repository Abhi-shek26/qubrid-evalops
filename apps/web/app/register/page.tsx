"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { API_URL } from "../../lib-api";
export default function Register(){const r=useRouter();const[n,setN]=useState("");const[e,setE]=useState("");const[p,setP]=useState("");const[err,setErr]=useState("");async function s(x:React.FormEvent){x.preventDefault();const q=await fetch(`${API_URL}/api/v1/auth/register`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:n,email:e,password:p})});const d=await q.json();if(!q.ok){setErr(d.message);return;}localStorage.setItem("evalops_token",d.data.token);r.push("/dashboard");}return <main><div className="card"><h1>Create account</h1><form onSubmit={s}><label>Name<input value={n} onChange={x=>setN(x.target.value)}/></label><label>Email<input value={e} onChange={x=>setE(x.target.value)} type="email"/></label><label>Password<input value={p} onChange={x=>setP(x.target.value)} type="password"/></label>{err&&<p>{err}</p>}<button>Register</button></form></div></main>}
